{
  "builds": [
    {
      "src": "index.js",
      "use": "@vercel/node",
      "config": {
        "production": {
          "command": "npm install --force && npm run build"
        }
      }
    }
  ],
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
